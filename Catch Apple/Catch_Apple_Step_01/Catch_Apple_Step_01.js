function setup() { 
  createCanvas(400, 400);
  background(220);
  } 

function draw() { 
  ellipse(15, 15, 20, 20);        // Fill shapes with pure red color
  rect(width / 2, height - 25, 20, 20); // Circle of 30 pixels diameter
}